{
  "name": "mern-redux-auth-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm run start-server",
    "start-client": "cd client && npm start",
    "start-server": "cd server && nodemon server.js",
    "dev": "concurrently \"npm run start-server\" \"npm run start-client\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}